#include <stdio.h>

    /* Operadores aritmeticos:
    soma (+), subtracao (-), mltiplicacao (*), divisao (/), modulo (%), potencia (**) */
    //Atencao a ordem de precedencia --> ( ), **, */, + - .

main () {

    int num1 = 10, num2 = 50, soma;

    /* Pratica ruim
    printf("Digite dois numeros inteiros: ");
    scanf("%d %d", &num1, &num2);*/

    soma = num1 + num2;

    printf("a soma: %d", soma);


}